// `new` on something with no constructor to run.

interface Shape {
    int area();
}

enum Color { Red, Green }

public void main() {
    var s = new Shape();
    var c = new Color();
    print(1);
}
