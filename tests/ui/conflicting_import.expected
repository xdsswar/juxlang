conflicting_import\app.jux:6:1: [E0303] error: conflicting imports: the name `Widget` is imported from both `alpha.Widget` and `beta.Widget` -- give one an `as` alias (`import beta.Widget as Widget2;`) or use a fully-qualified name
conflicting_import\app.jux:9:11: [E0416] error: cannot use package-private type `beta.Widget` from `app` -- it is declared in `beta` without `public`, so it is visible only inside that package (§4.4). Mark the declaration `public` to export it
