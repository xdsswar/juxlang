<scheme name="Dark" version="142" parent_scheme="Dark">
  <!-- New-UI "Dark" editor scheme: same dark blue type-parameter color as
       Darcula (the New UI uses a distinct scheme name, so it needs its own
       additionalTextAttributes entry or the default never applies). -->
  <attributes>
    <option name="JUX_TYPE_PARAMETER">
      <value>
        <option name="FOREGROUND" value="4a78c8"/>
      </value>
    </option>
  </attributes>
</scheme>
