package app.zoo;

/** A polymorphic base, named like the one in `app.garage`. */

/** A nested class named like the one in `app.garage.Pen`. */
public class Pen {
    public class Gate {
        public bool open;

        public Gate() {
            this.open = false;
        }
    }

    public Gate gate = new Gate();
}
